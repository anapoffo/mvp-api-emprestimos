version: "3.9"

services:
  api-principal:
    build: .
    ports:
      - "8000:8000"
    environment:
      - TEMPORALIDADE_API_URL=http://api-temporalidade:8001
    depends_on:
      - api-temporalidade

  api-temporalidade:
    build: ../api-temporalidade
    ports:
      - "8001:8001"
